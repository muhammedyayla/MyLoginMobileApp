import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'First_Mobile_Project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
